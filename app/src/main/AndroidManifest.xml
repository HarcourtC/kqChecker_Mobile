<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name="org.example.kqchecker.KqApplication"
        android:allowBackup="true"
        android:label="kqChecker"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
        <activity android:name="org.example.kqchecker.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WebView login activity; must be declared so ActivityManager can resolve it -->
        <activity
            android:name="org.example.kqchecker.auth.WebLoginActivity"
            android:exported="true"
            android:label="Web Login" />

        <!-- Foreground polling service (optional) -->
        <service android:name="org.example.kqchecker.sync.Api2PollingService" android:exported="false" />

        <!-- Restart polling service after device boot if user enabled it -->
        <receiver android:name="org.example.kqchecker.sync.BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
